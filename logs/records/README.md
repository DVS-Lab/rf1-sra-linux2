# Run Records

This directory is intentionally tracked. Use `code/run_logged.sh` to write a
small Markdown record for each logged pipeline run. The full raw logs remain
local under ignored `logs/runs/`.
